server:
  port: 8080
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              # SECURITY: tắt auto-route theo service-id. Nếu bật, gateway sinh route
              # /{service-id}/** KHÔNG qua filter JwtAuth → bypass xác thực/role và lộ
              # các endpoint nội bộ /api/internal/**. Chỉ dùng explicit routes bên dưới.
              enabled: false
              lower-case-service-id: true
          routes:
            - id: user-service-auth
              uri: lb://USER-SERVICE
              predicates:
                - Path=/api/users/register,/api/users/login,/api/users/refresh-token,/api/users/logout,/api/users/forgot-password,/api/users/reset-password,/api/users/verify-email,/api/users/resend-verification,/api/oauth/**
            - id: user-service-protected
              uri: lb://USER-SERVICE
              predicates:
                - Path=/api/users/me,/api/users/me/**,/api/vouchers,/api/vouchers/**,/api/admin/users,/api/admin/users/**,/api/admin/vouchers,/api/admin/vouchers/**
              filters:
                - JwtAuth
            - id: product-service-admin
              uri: lb://PRODUCT-SERVICE
              predicates:
                - Path=/api/admin/products,/api/admin/products/**,/api/admin/categories,/api/admin/categories/**,/api/admin/vendors,/api/admin/vendors/**
              filters:
                - JwtAuth
            - id: product-service
              uri: lb://PRODUCT-SERVICE
              predicates:
                - Path=/api/products,/api/products/**,/api/categories,/api/categories/**,/api/vendors,/api/vendors/**
            - id: order-service
              uri: lb://ORDER-SERVICE
              predicates:
                - Path=/api/orders,/api/orders/**,/api/staff/orders,/api/staff/orders/**,/api/admin/reports,/api/admin/reports/**
              filters:
                - JwtAuth
            - id: notification-service
              uri: lb://NOTIFICATION-SERVICE
              predicates:
                - Path=/api/notifications,/api/notifications/**
              filters:
                - JwtAuth
            # Callback VNPay redirect trình duyệt về (KHÔNG có JWT) — phải đứng trước
            # route catch-all bên dưới để khớp đầu tiên và bỏ qua JwtAuth.
            - id: payment-service-callback
              uri: lb://PAYMENT-SERVICE
              predicates:
                - Path=/api/payments/vnpay/callback
            # Mọi endpoint payment còn lại (create, tra cứu trạng thái, refund) đều cần JWT:
            # gateway inject X-User-Id/X-User-Role để service enforce ownership + role.
            - id: payment-service
              uri: lb://PAYMENT-SERVICE
              predicates:
                - Path=/api/payments/**
              filters:
                - JwtAuth
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOrigins: "http://localhost:5173"
                allowedMethods: "*"
                allowedHeaders: "*"
                allowCredentials: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

jwt:
  secret: ${JWT_SECRET:dev-secret-key-change-me-please-32bytes!!}

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
