# Copy this file to .env and adjust values for local demo only.
DB_PASSWORD=secret
JWT_SECRET=

# Google OAuth local dev.
# Client ID can be shared for the team dev OAuth client.
# Client Secret must be filled locally and must never be committed.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8080/api/oauth/google/callback
OAUTH_STATE_TTL_SECONDS=600

# Frontend links used in emails sent by notification-service.
RESET_PASSWORD_BASE_URL=http://localhost:5173/reset-password
VERIFY_EMAIL_BASE_URL=http://localhost:5173/verify-email
VERIFY_EMAIL_TTL_SECONDS=86400

# SMTP email sender. Defaults match local Mailhog; use provider credentials outside dev.
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=no-reply@cartzilla.local
MAIL_SMTP_AUTH=false
MAIL_SMTP_STARTTLS_ENABLE=false

# Set true to demo Saga rollback: payment-service always fails.
ALWAYS_FAIL=false

# VNPAY sandbox (test). vnp_ReturnUrl = backend callback (verify + settle), sau đó
# backend 302 redirect khách về VNPAY_FRONTEND_RETURN_URL (trang kết quả của SPA).
VNPAY_TMN_CODE=
VNPAY_HASH_SECRET=
VNPAY_RETURN_URL=http://localhost:8080/api/payments/vnpay/callback
VNPAY_FRONTEND_RETURN_URL=http://localhost:5173/payment/result
VNPAY_IPN_URL=http://localhost:8080/api/payments/vnpay/ipn
VNPAY_REFUND_URL=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
VNPAY_REFUND_ENABLED=false

# Host ports for database demo from your machine (mỗi DB một port riêng để tránh đụng nhau).
USER_DB_PORT=15432
PRODUCT_DB_PORT=5433
ORDER_DB_PORT=5434
PAYMENT_DB_PORT=5435
NOTIF_DB_PORT=5436

# Direct service ports khi chạy bằng Docker Compose.
USER_SERVICE_PORT=8081
PRODUCT_SERVICE_PORT=8082
ORDER_SERVICE_PORT=8083
PAYMENT_SERVICE_PORT=8084
NOTIFICATION_SERVICE_PORT=8085
